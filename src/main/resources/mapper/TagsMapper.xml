<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wuzt.myblog.dao.TagsDao">
    <resultMap id="BaseResultMap" type="com.wuzt.myblog.model.Tags">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="tag_name" property="tagName" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="blog_id" property="blogId" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    <sql id="Base_Column_List">
        tags.id, tags.tag_name, tags.user_id, tags.create_time
    </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from tags
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete from tags
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.wuzt.myblog.model.Tags" keyColumn="id"
            keyProperty="id" useGeneratedKeys="true">
        insert into tags (id, tag_name, user_id, create_time)
        values (#{id,jdbcType=INTEGER}, #{tagName,jdbcType=VARCHAR}, #{userId,jdbcType=INTEGER},
        #{createTime,jdbcType=TIMESTAMP})
    </insert>
    <insert id="insertSelective" parameterType="com.wuzt.myblog.model.Tags">
        insert into tags
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="tagName != null">
                tag_name,
            </if>
            <if test="userId != null">
                user_id,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="tagName != null">
                #{tagName,jdbcType=VARCHAR},
            </if>
            <if test="userId != null">
                #{userId,jdbcType=INTEGER},
            </if>
            <if test="createTime != null">
                #{createTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.wuzt.myblog.model.Tags">
        update tags
        <set>
            <if test="tagName != null">
                tag_name = #{tagName,jdbcType=VARCHAR},
            </if>
            <if test="userId != null">
                user_id = #{userId,jdbcType=INTEGER},
            </if>
            <if test="createTime != null">
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.wuzt.myblog.model.Tags">
        update tags
        set tag_name = #{tagName,jdbcType=VARCHAR},
        user_id = #{userId,jdbcType=INTEGER},
        create_time = #{createTime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=INTEGER}
    </update>

    <select id="selectByCondition" resultMap="BaseResultMap" parameterType="com.wuzt.myblog.model.Tags">
        select
        <include refid="Base_Column_List"/>
        from tags
        left join blog_tag on blog_tag.tag_id = tags.id
        <where>
            <if test="userId!=null and userId!=0">
                and tags.user_id = #{userId,jdbcType=INTEGER}
            </if>
            <if test="blogId!=null and blogId!=0">
                and blog_tag.blog_id = #{blogId,jdbcType=INTEGER}
            </if>
        </where>
        group by tags.id
    </select>
    <!-- 根据tagName,userId查询tag数量 -->
    <select id="selectCountByName" resultType="java.lang.Integer" parameterType="java.util.HashMap">
        select
        <include refid="Base_Column_List"/>
        from tags
        where tag_name = #{tagName,jdbcType=VARCHAR} and user_id = #{userId,jdbcType=INTEGER}
    </select>

    <insert id="insertBlogTag" parameterType="com.wuzt.myblog.model.BlogTag">
        insert into blog_tag (id, blog_id, tag_id)
        values (#{id,jdbcType=INTEGER}, #{blogId,jdbcType=INTEGER}, #{tagId,jdbcType=INTEGER})
    </insert>
</mapper>